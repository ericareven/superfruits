<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <title></title>
    </head>
    <body>
        <h1>Fruits index page</h1>
        <button><a href="/fruits/new">Creat New Fruit</a></button>
        <ul>
            <% for(let i = 0; i < fruits.length; i++){ %>
                <li>
                    <button><a href="/fruits/<%=fruits[i].id; %>/edit">Edit</a></button>
                    <br>
                    The 
                    <a href="/fruits/<%=fruits[i].id %>">
                    <%=fruits[i].name; %></a> 
                    is  <%=fruits[i].color; %>.
                    <% if(fruits[i].readyToEat === true){ %>
                        It is ready to eat
                    <% } else { %>
                        It is not ready to eat
                    <% } %>
                    <form action="/fruits/<%=fruits[i].id%>?_method=DELETE" method="POST">
                        <input type="submit" value="Delete">
                    </form>
                    <br>
                </li>
            <% } %>
        </ul>
    </body>
</html>